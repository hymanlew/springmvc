package mvc.servlet;

public class Source {
    /**
     * WEB 的三大组件：
     * servlet（包括 DispatcherServlet），filter，listener（它们都要在 web.xml 中进行配置的）。
     *
     * 而在 servlet3.0 版本以后，它提供了 web 的注解版的配置，而不用再使用 web.xml 进行配置了。
     * 需要注意的是，servlet3.0 是 JSR315 的规范，需要 tomcat7 及以上版本才能支持。
     */
}
